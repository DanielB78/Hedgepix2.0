# Deprecated

This script synced from the Bargo Congress Trades API.

Congressional trade ingestion now runs via the local backend updater:

```bash
cd backend
npm run update-data
```

Or double-click `update.bat` on Windows.

See the root README section **Updating congressional trade data**.
